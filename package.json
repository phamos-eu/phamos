{
	"name": "phamos",
	"private": true,
	"scripts": {
		"postinstall": "cd frontend && yarn install --check-files && cd ../hr_frontend && yarn install --check-files",
		"dev": "cd frontend && yarn dev",
		"dev:hr": "cd hr_frontend && yarn dev",
		"build": "cd frontend && yarn build && cd ../hr_frontend && yarn build"
	}
}
