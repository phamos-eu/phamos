{
	"name": "phamos",
	"private": true,
	"scripts": {
		"postinstall": "cd frontend && yarn install --check-files && cd ../hr_frontend && yarn install --check-files && cd ../sales_frontend && yarn install --check-files && cd ../accounting_frontend && yarn install --check-files && cd ../project_management_frontend && yarn install --check-files",
		"dev": "cd frontend && yarn dev",
		"dev:hr": "cd hr_frontend && yarn dev",
		"dev:sales": "cd sales_frontend && yarn dev",
		"dev:accounting": "cd accounting_frontend && yarn dev",
		"dev:pm": "cd project_management_frontend && yarn dev",
		"build": "cd frontend && yarn build && cd ../hr_frontend && yarn build && cd ../sales_frontend && yarn build && cd ../accounting_frontend && yarn build && cd ../project_management_frontend && yarn build"
	}
}
